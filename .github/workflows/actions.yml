name: Github actions
on:
    push:
        branches:
            - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: test_name
        run: ls -lah
        # uses: 
    
  test_s:
    runs-on: ubuntu-latest
    needs: [build]

    steps:
      - name: make_depend
        run: ls -lhi